<%= form_for(@vehicle) do |f| %>
  <% if @vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% @vehicle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :vehicle_plate %><br>
    <%= f.text_field :vehicle_plate %>
  </div>  
  <div class="field">
    <%= f.label :instructor_id %><br>
  	<%= collection_select(:vehicle, :instructor_id, @instructors, :id, :name) %>    
  </div>
  <div class="field">
    <%= f.label :show_order %><br>
    <%= f.number_field :show_order %>
  </div>      
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>   
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
